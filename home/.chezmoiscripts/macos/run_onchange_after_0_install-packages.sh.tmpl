#!/bin/bash

# Checksum to trigger brew bundle when packages.yaml changes
# {{ .packages.macos.brew | toJson | sha256sum }}

echo "> Running $(basename "$0") "

set -eufo pipefail

brew bundle --global
